[project]
name = "school-management"
version = "0.1.0"
description = "Django Student Management System"
requires-python = ">=3.13"
dependencies = [
    "django",
    "whitenoise",
    "dj-database-url"
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk"
]